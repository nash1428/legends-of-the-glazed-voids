# Optional: enables live Claude Judge + Actor. Without it the game runs a
# deterministic heuristic judge + in-character templated actor (fully playable).
# NOTE: any VITE_ var is embedded in the client bundle and is publicly visible.
VITE_ANTHROPIC_API_KEY=
